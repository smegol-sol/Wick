{
  "name": "@wick/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chain-agnostic contracts and the pure trading logic shared by the desk and the engine.",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --import ../../scripts/test-loader.mjs --test src/*.test.ts"
  },
  "dependencies": {
    "@noble/curves": "^2.4.0"
  },
  "devDependencies": {
    "@solana/web3.js": "^1.98.4"
  }
}
